"""
Class Dispatcher
__init__ files in sub-modules should import all necessary components from within their class, so upper level
classes can simply import the class (ex: `from .classes import Person` would work because of this file)
"""

from .person_object import Person